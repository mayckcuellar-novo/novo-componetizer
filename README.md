# novo-componetizer
Claude Code plugin that conforms Figma frames to the Novo Design System — binds DS text styles, color variables, and swaps raw layers for DS components, without changing the visual design.
